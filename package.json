{
  "name": "displate-scraper",
  "version": "1.0.0",
  "description": "a scraper to download images from displate",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist --copy-files",
    "start": "npm run build && node --experimental-modules dist/index.js"
  },
  "keywords": [
    "automation"
  ],
  "author": "Jaidev",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.16.0",
    "@babel/core": "^7.16.0",
    "@babel/preset-env": "^7.16.4"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.10",
    "puppeteer": "^11.0.0",
    "request": "^2.88.2"
  }
}
